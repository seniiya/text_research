# 코딩북 제4부 — 사용자 화행 · 감정 표면성 (v2)

> 이 문서는 참조용 기준 문서다. 코드 부여는 사람이 한다. LLM에 맡기지 않는다.
>
> 대상은 `role = Prompt`(사용자 발화)를 **문장 단위로 나눈 것**이다.
> 제1부(AI 응답 전략)·제3부(사용자 자기개방)와 단위가 다르다. 아래 「운영 메모」 참조.
>
> **v2 변경 요약** — 대화행위 3클래스(`PHATIC`·`RESP`·`EXPR`) 추가, 판정 우선순위 신설.
> 근거는 맨 아래 「변경 이력」에 있다.

---

## 축 A. 화행 (Speech Act) — 9클래스 + UNC

두 계보를 합친다. **논문에 이 사실과 합친 이유를 반드시 밝힌다.**

| 출처 | 가져온 것 |
|---|---|
| Cho, W. I., Lee, H. S., Yoon, J. W., Kim, S. M., & Kim, N. S. (2018). *Speech Intention Understanding in a Head-final Language: A Disambiguation Utilizing Intonation-dependency.* arXiv:1811.04231. | `FRAG` `S` `Q` `C` `RQ` `RC` (3i4K 7클래스 중 IU 제외 — 텍스트 전용이라 억양 의존 판별 불가) |
| ISO 24617-2:2012 (rev. 2020). *SemAF — Part 2: Dialogue acts.*<br>Bunt, H., et al. (2012). LREC 2012. | `PHATIC` (Social Obligations Management) |
| Core, M. G., & Allen, J. F. (1997). *Coding Dialogs with the DAMSL Annotation Scheme.* AAAI Fall Symposium.<br>Stolcke, A., et al. (2000). *Dialogue Act Modeling…* Computational Linguistics, 26(3), 339–373. | `RESP` (Answer, Accept/Reject) |
| Searle, J. R. (1976). *A Classification of Illocutionary Acts.* Language in Society, 5(1), 1–23. | `EXPR` (Expressive) |

이론적 배경: 3i4K는 Portner의 담화 성분(Common Ground / Question Set / To-Do List)을
화행 수준으로 확장한 체계다. **여기에는 '표출'도 '친교'도 없다.** 또한 3i4K는 고립된
문장 하나를 대상으로 하므로 '앞 발화에 대한 응답'이라는 범주가 없다. 본 연구는 다중턴
대화를 다루므로 그 셋을 대화행위 표준과 Searle에서 보충한다.

**판정 기준: 문장 형식이 아니라 실제 기능**

| 코드 | 이름 | 기준 |
|---|---|---|
| FRAG | Fragment | **절이 완결되지 않은 발화. 길이와 무관** ("어제 회사에서") |
| RESP | Response | 직전 AI 발화에 대한 대답·수락·거절 |
| PHATIC | Phatic | 인사·작별·감사·사과 |
| EXPR | Expressive | 감정·평가의 분출이 발화의 목적 |
| Q | Question | 실제로 답을 요구 |
| C | Command | 행동·수행을 요구 |
| RQ | Rhetorical Question | 의문문 형식이나 답을 요구하지 않음 |
| RC | Rhetorical Command | 명령문 형식이나 실제 지시가 아님 |
| S | Statement | 정보·상태 서술 |
| UNC | Unclassifiable | 위 어디에도 안 맞음 (이유를 note에 기록) |

### 판정 우선순위 — 위에서부터 먼저 걸리는 규칙을 따른다

```
1. 절이 완결되지 않았다                  → FRAG
2. 직전 AI 발화에 대한 대답·수락·거절이다   → RESP
3. 인사·작별·감사·사과다                 → PHATIC
4. 감정·평가의 분출이 발화의 목적이다       → EXPR
5. 실제로 답을 요구한다                  → Q
6. 행동·수행을 요구한다                  → C
7. 의문문 형식이나 답 요구가 없다          → RQ
8. 명령문 형식이나 실제 지시가 아니다       → RC
9. 정보·상태를 서술한다                  → S
10. 위에 해당 없음                      → UNC
```

> **`RESP`가 `EXPR`보다 앞선다.** "아니 지랄 ㄴ"처럼 응답이면서 표출인 발화는
> `RESP`로 간다. 정서는 **축 B가 이미 잡기 때문이다**(그 발화는 `EXP`). 축 A까지
> 정서를 기준으로 두면 두 축이 같은 것을 재게 되어, 축을 둘로 나눈 의미가 사라진다.

### 판정 규칙 — 실제 코딩에서 갈린 지점

우선순위만으로는 안 갈린 경계다. `P03`·`P04`·`P14`·`P15`에서 같은 종류의 흔들림이
반복돼 규칙으로 고정했다. 근거는 「변경 이력」의 v2.1 참조.

**규칙 1 — `S` / `EXPR`은 서술어로 가른다**

| 서술어가 | 코드 | 예 |
|---|---|---|
| 화자의 **감정·평가** | `EXPR` | 싫다 · 좋다 · 힘들다 · 덥다(체감) · 걱정이다 |
| **사실·상황** | `S` | 일요일이다 · 마요네즈가 없다 · 본 적이 없다 |

**욕구형(`~싶다`·`~좋겠다`)은 `EXPR`이다.** 정보 전달이 아니라 화자의 태도 표출이며,
상담 대화에서 핵심 발화 유형이기 때문이다.

> 단 `~싶다`가 **추측**을 뜻하면 `S`다. "어떻게 그렇게 못하나 싶다"는 욕구가 아니다.
> 문장에 서술어가 여럿이면 **주 서술어**로 판정한다. "보고 싶은데 본 적이 없어"는
> 주 서술어가 "본 적이 없어"(사실)이므로 `S`.

**규칙 2 — `Q` / `C`는 무엇으로 충족되는지로 가른다**

- **답변으로 충족되면 `Q`** — "추천해줘", "알려줘" (명령형이어도 `Q`)
- **행위 수행이 필요하면 `C`** — "길게 답장은 하지 마", "응원 좀 해봐"

한국어 채팅은 정보 요청을 명령형으로 하는 일이 매우 잦다. **형식이 아니라
기능으로 판정한다**는 대원칙을 여기 그대로 적용한 것이다.

**규칙 3 — `RQ`·`RC`는 형식 요건이 있다**

`RQ`는 **의문문**, `RC`는 **명령문** 형식일 때만 쓴다. 평서문은 아무리 기능이
질문·지시에 가까워도 해당하지 않는다.

> "너는 안가잖아", "고민이야"는 평서문이므로 `RQ`가 될 수 없다.
> 원 체계(3i4K)가 `RQ`·`RC`를 나눈 근거는 **어조**였고, 형식 요건은 그 잔여다.

**규칙 4 — `PHATIC`은 인사가 발화의 목적일 때만**

문두에 붙은 인사·감사는 **무시하고 주 기능으로 판정**한다.

```
안녕 지피티야                          → PHATIC   (인사 단독)
안녕 퇴사하고 싶어                      → EXPR     (인사는 부수적)
고마워 곧 유럽여행가는데 ... 걱정이네     → EXPR
```

우선순위상 `PHATIC`이 3번이지만, 이 규칙이 우선순위보다 앞선다.

### `FRAG`와 `UNC`의 구분

둘 다 "코드를 못 주겠다"로 보이지만 **막히는 지점이 다르다.**

| | `FRAG` | `UNC` |
|---|---|---|
| 막히는 곳 | **형태** — 절이 완결되지 않음 | **기능** — 완결됐는데 분류가 안 됨 |
| 성격 | 원 분류 체계의 정식 클래스 | 본 연구가 더한 잔여 범주 |
| 높으면 뜻하는 것 | 말투가 파편적이다 → **데이터 사실** | 체계가 데이터를 못 덮는다 → **도구 결함** |

**결과 해석이 정반대이므로 반드시 갈라야 한다.** 한 칸에 섞이면 둘 중 어느 말도 못 한다.
"짧다"는 `FRAG`의 근거가 아니다. 한 어절이어도 기능이 잡히면 `RESP`나 `S`다.

경계 사례:
- "모닝콜 해줄 수 있어?" → 형식 의문문, 기능 요청 → **C**
- "내가 뭘 그렇게 잘못했냐" → 형식 의문문, 답 요구 없음 → **RQ**
- "소고기" (AI가 "뭐 먹어?"라고 물은 직후) → 규칙 2가 먼저 걸린다 → **RESP**
- UNC는 억지로 채우지 말 것. UNC 비율 자체가 결과다.

## 축 B. 감정 표면성 (Emotion Surface) — 3클래스

본 연구에서 새로 정의. 화행 분류가 포착하지 못하는
'정서적 간접성'을 별도 축으로 분리하기 위함.

| 코드 | 기준 |
|---|---|
| EXP | 감정어·감정 표현이 표면에 명시됨 ("너무 힘들어") |
| IMP | 표면에 감정어는 없으나 맥락상 정서 상태가 읽힘 ("그냥 뭐 괜찮아요") |
| NONE | 정서와 무관한 발화 (정보 요청 등) |

IMP로 판정한 경우 note에 단서를 1문장으로 기록할 것.

## 코딩 절차
1. 코더 2명이 독립적으로 30개 전체를 코딩
2. 두 축 각각에 대해 Cohen's κ 산출
3. 불일치 항목을 함께 검토하고 코드북 수정
4. **LLM은 이 단계에 관여하지 않는다**

---

## 운영 메모 — 이 저장소에서 실제로 적용되는 사항

위 본문이 기준이고, 아래는 그것을 이 데이터에 적용할 때의 절차다.

### 코딩 단위 — 제2부·제3부와 충돌하는 지점

코딩북 제2부 14항과 제3부 2항은 **"사용자 발화를 문장 단위로 분리하지 않는다"**를
명시한다. 제4부는 그 규칙의 예외다. 화행은 발화가 아니라 문장에 붙는 속성이고,
한 턴 안에 서술과 요청이 섞이는 사례가 흔하기 때문이다
(예: `배고프다 점메추` = 상태 서술 + 요청).

- 제4부의 단위는 **문장**이다.
- 제3부(자기개방)의 단위는 여전히 **발화 1건**이다. 두 값을 같은 행에 놓지 않는다.
- 논문에는 단위가 축마다 다르다는 사실과 그 이유를 명시한다.

### 문장 분리 규칙

제1부 3항을 따르되 사용자 발화용으로 두 가지를 바꾼다
(`scripts/make_speechact_pilot.py`).

| | 제1부 3항 (AI 응답) | 제4부 (사용자 발화) |
|---|---|---|
| 선두 불릿 | 제거한다 | **제거하지 않는다** — 원문 훼손 |
| 목록 번호 `1.` `2.` | 해당 없음 | **문장 경계로 세지 않는다** |

목록 번호를 경계로 두면 `1.` 하나가 문장 하나가 되어 FRAG가 인위적으로 불어난다.
P20에서 이 규칙 하나로 문장 수가 159 → 145로 줄었다. 그 14개는 전부 가짜 조각이었다.

### 종결어미 분리는 자동화하지 않는다

한국어 종결어미는 문중 형태와 겹친다(`다`=부사, `지`=어미/명사). 정규식으로 자르면
`우리반애들 다 빡대갈이라` 가 `우리반애들 다` / `빡대갈이라` 로 깨진다.

스크립트는 **후보만 콘솔에 표시**하고 자동 분리하지 않는다. 더 나눌지는 조정
회의에서 정하고, 확정한 규칙을 이 항에 추가한다.

### 블라인드

제2부 15항("서로의 결과를 보지 않는다")을 지키려면 코더에게 한 파일을 같이 주면 안
된다. 그래서 산출물이 두 갈래다.

| 파일 | 용도 |
|---|---|
| `data/coding/<코더>/P##_speechact.xlsx` | **코더가 여는 파일.** 자기 열만 있음. 드롭다운·시트보호 |
| `data/coding/<코더>/P##_speechact.csv` | 같은 내용의 Git 확인용 사본 |
| `data/output/pilot_speechact/pilot_coding_sheet_P##.csv` | **병합·κ 산출용.** 두 코더 열이 한 파일. 코더에게 주지 않는다 |

제1부 시트와 같은 폴더에 두되 파일명으로 축을 구분한다.
`P04.xlsx` = 제1부(AI 응답 전략), `P04_speechact.xlsx` = 제4부(화행·감정 표면성).
**제1부 시트에는 화행 열이 없다.** 그쪽 사용자 발화 행은 `esconv='-'` 로 잠긴
문맥 행이고 문장 분리도 되어 있지 않다.

**참여자 본인은 자기 파일을 코딩하지 않는다.** coder1이 `P01`이므로 `P01`은
모집단에서 제외되어 있다(`EXCLUDE`). 선정한 참여자가 coder2 본인이 아닌지
`data/private/mapping.csv`로 먼저 확인한다.

### `prev_assistant_turn` — 턴의 첫 문장에만 적는다

맥락은 **턴 단위**인데 행은 **문장 단위**다. 한 턴이 2문장이면 같은 GPT 응답이 두 번
찍히므로, 첫 문장에만 적고 나머지는 비운다. P04에서는 5개 턴(8·10·11·12·13)이
해당한다.

```
P04-t12-s01  [맥락 201자]  아니 지랄 ㄴ
P04-t12-s02  [       비움]  야 이등변 밑각 두개 같은거 누가 몰라? 그거 못 구한다고
```

그 대신 **행 순서를 바꾸면 뒤 문장이 맥락을 잃는다.** 그래서 제1부 시트와 달리
정렬·필터를 시트 보호로 잠가 두었다. 맥락이 필요하면 `utterance_id`의 `t##`로
같은 턴의 첫 행을 찾는다.

### 재생성과 동기화

```bash
python scripts/make_speechact_pilot.py --pid=P04     # 한 참여자 (파일럿)
python scripts/make_speechact_pilot.py --all         # 19명 층화 30개
python scripts/make_speechact_pilot.py --pid=P20 -n=30

python scripts/sync_labels.py P04_speechact --coder=coder1   # xlsx -> csv
python scripts/sync_labels.py --all                          # 제1부·제4부 전부
```

`seed=42` 고정. `P01`은 어느 모드에서도 제외된다.
스크립트는 **라벨을 부여하지 않는다.** 추출·분리·표본추출만 한다.

**라벨 정본은 XLSX다.** 제1부와 같다. 코더는 엑셀에서 입력하고, CSV는
`sync_labels.py`를 돌려야 따라온다. `sync_labels.py`는 파일명으로 시트 종류를
가른다 — `P##.xlsx`는 `(turn, sent_no)` 키에 `esconv`를, `P##_speechact.xlsx`는
`utterance_id` 키에 `speech_act_*`·`emotion_surface_*`·`note`를 옮긴다.
어느 쪽이든 **`text`가 다르면 그 행은 건너뛴다.**

> ⚠️ 재생성(`make_speechact_pilot.py`)은 시트를 **통째로 새로 만든다.**
> 제1부의 `carry_over()` 같은 라벨 이어받기가 없다. 코딩을 시작한 뒤에는
> 다시 돌리지 말고, 돌려야 하면 `sync_labels.py`를 먼저 실행해 CSV에 라벨을
> 남겨 둔다.

### 파일럿 한계 — 논문에 그대로 밝힐 것

단일 참여자 표본이므로 여기서 나온 κ는 **코딩북이 이 참여자의 말투에 대해
작동하는지**만 말해 준다. 전체 612문장으로 일반화되지 않는다. 파일럿 목적은
코드 경계의 구멍을 찾는 것이지 신뢰도 보고가 아니다. 신뢰도 보고용 표본은
`--all`(층화 30개)로 따로 뽑는다.

---

## 변경 이력

### v2.1 — 판정 규칙 4개 추가 (클래스 변경 없음)

**근거: `P03`·`P04`·`P14`·`P15`·`P16`·`P17` 66문장 코딩 중 발견.**

코드 목록은 그대로 두고, 우선순위만으로 안 갈리던 경계에 규칙을 넣었다.
같은 현상이 **참여자를 넘어 반복해서 다르게 판정된 것**이 근거다.

| 규칙 | 반복된 흔들림 |
|---|---|
| 1. `S`/`EXPR` | `더워서 더 싫다`→`S` vs `오늘 출근하기 싫다`→`EXPR` (3명에서 6회) |
| 2. `Q`/`C` | `추천해줘바`→`Q`(P03) vs `추천해줘.`→`C`(P15) |
| 3. `RQ` 형식 | 평서문에 `RQ`를 준 사례 3건 (`너는 안가잖아`, `고민이야`) |
| 4. `PHATIC` | 문두 인사가 붙은 복합문에서 우선순위와 실제 판정이 어긋남 |

규칙 적용 후 66문장 기준 `UNC` 0건 · `RQ` 0건.

> **`RQ`가 0이 된 것은 주목할 결과다.** 원 체계에서 `RQ`·`RC`를 별도 클래스로 둔
> 근거는 **어조**였다. 텍스트 전용 데이터에는 그 근거가 없으므로 두 코드가 비는
> 것이 자연스럽다. 최종 보고 시 `RQ`·`RC`의 실제 사용률을 근거로 다룬다.

### v2 — `PHATIC` · `RESP` · `EXPR` 추가

**근거: P04 파일럿 1인 코딩 결과 (coder1, 28문장).**

| 관찰 | 수치 |
|---|---|
| 사용된 코드 | 7개 중 **4개** (`FRAG`·`C`·`RC` 0건) |
| `S` 편중 | 16/28 = **57%** |
| `UNC` | 7/28 = **25%** — 제1부 7항의 `OTH` 진단선 30%에 근접 |

`UNC` 7건이 무작위로 흩어지지 않고 **세 종류로 뭉쳤다.**

| 종류 | 발화 | coder1 note |
|---|---|---|
| 친교 | 안녕 지피티야 | "인사" |
| 표출 | 오늘 출근하기 싫다 · 아니 지랄 ㄴ | "본인의 감정표현", "욕설" |
| 응답 | 됐다 안할래 · 부드럽게 · 엉 | "답변", "gpt의 말에 대답하는 응답" |

**같은 현상이 다른 코드로 간 쌍이 두 개 있었다** (코더 1인 내부 비일관):

```
감정 표출   오늘 출근하기 싫다 → UNC   /   ㅇ니 자고싶음·아너무덥다 → S
한 단어 대답  부드럽게 → UNC          /   소고기 → S
```

체계가 데이터를 못 덮어서 코더가 같은 것을 다르게 처리한 것이다. 이 상태로 2인
코딩에 들어가면 κ가 무너진다. 그래서 **coder2 코딩 전에** 체계를 고쳤다.

세 클래스 모두 새로 만든 것이 아니라 기존 표준에서 가져왔다(위 출처표 참조).
다만 **`EXPR`은 대화행위 표준에 없다.** DAMSL·ISO 24617-2 어느 쪽도 Expressive를
독립 범주로 두지 않으므로 Searle(1976)에서 가져왔다. 이 계보 차이를 논문에 밝힌다.

> ⚠️ **v1 라벨은 그대로 쓸 수 없다.** `UNC` 7행뿐 아니라 `S` 16행도 정보 서술 /
> 표출 / 응답으로 갈리므로 재검토 대상이다. `Q`·`RQ` 5행만 영향이 없다.

### v1 — 최초 작성

3i4K 6클래스 + 감정 표면성 3클래스.
