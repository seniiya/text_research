# 코딩북 제2부 — 사용자 측 코딩

> 이 문서는 참조용 기준 문서다. 지시는 별도 프롬프트로 전달한다.
> 제2부는 사람이 판정한다. LLM에게 코드 부여를 맡기지 않는다.

---

## 코딩 대상 요약

| 항 | 대상 | 단위 | 대략 분량 |
|---|---|---|---|
| 9~11 | 에피소드 주제·목적·소재 | 에피소드 | 약 15~20개 |
| 12 | 사용자 Pushback | 사용자 발화 | 약 130개 (Y/N) |

사용자 감정(VA)은 코딩하지 않는다. 사후 설문의 에피소드 단위 자기보고를 사용한다.

---

## 9. 에피소드 정의 및 분할

에피소드란 하나의 대화 안에서 연속적으로 다루어진 하나의 주제 덩어리다.
한 참가자의 대화는 보통 1~3개의 에피소드로 나뉜다.

### 분할 규칙

1. 사후 설문 Q2(가장 오래 다룬 주제)와 Q4(두 번째 주제)를 먼저 확인한다.
   **참가자 본인의 구분을 우선한다.**
2. 설문에 없는 주제가 대화에 나타나면 에피소드를 추가한다.
3. 주제가 바뀌는 지점의 turn 번호를 경계로 기록한다 (`turn_start`, `turn_end`).
4. 3턴 미만의 짧은 곁가지는 별도 에피소드로 세지 않고 앞 에피소드에 포함한다.
5. 경계가 애매하면 표시해 두고 조정 회의에서 확정한다.

> `turn_start` / `turn_end`는 설문의 에피소드별 자기보고 VA를 대화 로그와 연결하는
> 유일한 고리다. 이 값이 없으면 에피소드별 전략 비율을 계산할 수 없다. 반드시 기록한다.

---

## 10. 대화 목적 코딩 (에피소드 단위)

### 코드 체계의 근거

사전 설문 Q4(평소 AI에게 주로 어떤 대화나 질문을 하나요)의 선택지를 그대로 코드로
사용한다. 새 체계를 만들지 않는 이유는 명확하다. 참가자의 자기보고(평소)와 대화
로그에서 관찰된 실제 행동을 대조하려면 두 자료가 같은 분류를 공유해야 한다.

단 한 가지만 수정한다. 설문의 "고민 상담" 항목은 조언 요청과 정서적 지지 요청을
구분하지 않으므로, 코딩 단계에서 두 코드로 분리한다. **이 분리가 본 연구의 코딩 기여다.**

### 코드 목록

| 코드 | 설문 Q4 선택지 | 판정 신호 | 예시 |
|---|---|---|---|
| `INFO` | 정보 검색·질문 | 사실·수치·방법을 물음. 의문형 중심 | "강릉까지 버스 몇 시간 걸려?" |
| `WORK` | 학업·업무 도움 | 과제·업무 수행 관련 요청 | "과제 수행에서의 문제" |
| `WRITE` | 글쓰기 도움 | 문서 작성·교정 요청 | "논문 읽기" |
| `CODE` | 코딩·기술 | 코드 작성·오류 해결 요청 | "코드 짜기" |
| `CHAT` | 일상 잡담 | 특정 목적 없는 대화 | "점심 뭐 먹을지" |
| `ADV` | 고민 상담 → 조언 요청 | 자기 상황을 설명하고 어떻게 할지 물음 | "이 일정인데 어떻게 하는 게 좋을까?" |
| `ES` | 고민 상담 → 정서적 지지 요청 | 감정 진술이 중심. 해결책을 묻지 않음 | "요즘 잘 움직이지도 않고 배가 자꾸 나오는 거 같아서 너무 걱정이야" |
| `PLAY` | 창작·놀이 | 창작물 생성, 역할놀이 | — |
| `ETC` | 기타 | 위에 해당 없음 | — |

- 에피소드마다 코드 하나만 부여한다.
- 판정 질문: **"사용자가 이 에피소드에서 원한 이상적인 응답은 무엇인가?"**
- 두 목적이 섞이면 주된 것을 `purpose`에, 부차적인 것을 `purpose_secondary`에 기록한다.

### ADV와 ES 구분 (가장 중요한 경계)

| | `ADV` (조언 요청) | `ES` (정서적 지지 요청) |
|---|---|---|
| 문장 형식 | 의문형 — "어떻게 해야 해?" | 서술형 — "~해서 힘들어" |
| 요청 대상 | 방법·판단 | 없음 (상태 진술만) |
| 감정어 | 없거나 부수적 | 발화의 중심 |
| 응답 후 반응 | "고마워, 해볼게" | "그니까…", 하소연 지속 |

### 평소와 실제를 따로 기록하는 방법

같은 9개 코드를 쓰되, 기록하는 단위와 위치가 다르다.

| | 평소 (자기 인식) | 실제 (관찰된 행동) |
|---|---|---|
| 출처 | 사전 설문 Q4 체크 항목 | 대화 로그 판정 |
| 단위 | 참가자 1명 | 에피소드 1개 |
| 개수 | 복수 가능 (파이프로 연결) | 1개 (부차적은 별도 열) |
| 기록 위치 | 표 B의 `usual_purpose` 열 | 표 A의 `purpose` 열 |
| ADV/ES 구분 | 불가 (설문은 "고민 상담" 하나) | 가능 (로그에서 판정) |

**대조 결과 예시**

| p_id | 평소 (Q4) | 실제 에피소드 | 대조 |
|---|---|---|---|
| P04 | INFO \| CODE \| 고민상담 | CHAT, ES | 잡담을 체크하지 않았으나 실제로는 잡담 에피소드가 있었음 |
| P08 | WORK \| CODE | WORK | 일치 |

> 설문 Q4는 복수 선택이므로 "평소" 값은 참가자당 여러 개다.
> 일치·불일치를 이진 판정하지 말고, **실제 에피소드 코드가 평소 체크 항목에
> 포함되는지 여부**로 기술한다.

---

## 11. 소재 코딩 (에피소드 단위)

소재는 에피소드가 다루는 생활 영역이다. 사전 설문에는 소재를 묻는 문항이 없으므로,
대화 로그와 사후 설문 Q2·Q4의 자유기술에서 귀납적으로 도출한 아래 코드를 사용한다.

> 10항의 목적(`purpose`)과 혼동하지 말 것. 목적은 "사용자가 무엇을 원했는가",
> 소재는 "무엇에 관한 이야기인가"다.
> 예: "과제가 너무 많아 힘들다" → 목적 `ES`, 소재 `CAREER`.

| 코드 | 소재 | 수집 데이터 예시 |
|---|---|---|
| `HEALTH` | 건강·신체 | 뱃살 걱정, 수면 관리, 다이어트, 목에 담 |
| `CAREER` | 진로·학업 | 미래 연구 분야 결정, 과제 수행 문제 |
| `SOCIAL` | 대인관계 | 친구·동료와의 갈등 |
| `FAMILY` | 가족 | 가족 여행 일정 부담 |
| `JOB` | 직무·감정노동 | 가르치는 학생 이야기, 야근 일정 |
| `DAILY` | 일상·취미 | 점심 메뉴, 강아지, 기기 개통 |
| `MONEY` | 금전 | — |
| `ETC` | 기타 | 위에 해당하지 않는 경우 |

- 사후 설문 Q2·Q4에 참가자가 직접 쓴 주제 표현은 `topic_self` 열에 **원문 그대로**
  보존한다. 요약하거나 정리하지 않는다.
- 목록에 없는 소재가 나오면 `ETC`로 두고 `topic_self`에 원문을 남긴다.
  `ETC`가 3건 이상 누적되면 코드를 신설하고 코딩북 버전을 올린다.

> 본 코드 목록은 수집된 11명 데이터에서 관찰된 소재를 귀납적으로 정리한 것이며,
> 확립된 외부 분류 체계를 차용한 것이 아니다. 논문에는 이 사실과 도출 절차를 명시한다.

---

## 12. Pushback 코딩 (사용자 발화 단위)

사용자가 **AI의 응답 방식**에 대해 명시적으로 불만을 표시한 발화를 표시한다.
사용자 발화 하나마다 Y 또는 N을 부여한다.

### 판정 원칙

- **명시적으로 언어화된 불만만** Y로 한다.
- 연구자가 "답답해 보인다"고 추론한 것은 Y로 하지 않는다.
- 대화 **주제**에 대한 분노(예: 학생·가족에 대한 짜증)는 pushback이 아니다.
  **AI 응답을 향한 것만** 해당한다.

> 이 구분이 핵심이다. 주제에 대한 분노와 AI에 대한 분노는 결과가 정반대로 나타난다.
> 관찰된 사례에서 주제에 화난 참가자는 24턴까지 대화를 이어갔고,
> AI에 화난 참가자는 6턴에서 중단했다.

### 유형 코드

| 코드 | 유형 | 수집 데이터 예시 |
|---|---|---|
| `VERBOSE` | 응답이 너무 길다 | "근데 너도 말이 너무 많다 어차피 고통 받는건 난데 잠자코 들어" / (첨언) "마지막엔 답변이 너무 길어서 화가 나서 그만 뒀음" |
| `UNSOL_ADV` | 요청하지 않은 조언·해결책 | (첨언) "길게 답변하고, 움직이기 싫은데 살 빼는 법에 대한 방법을 제안하려고 해서" |
| `CONTRA` | 사용자 진술을 반박·정정함 | "아니 지랄 ㄴ" (GPT가 "지능 문제로 단정하기 어렵다"고 반박한 직후) |
| `REPEAT` | 같은 말 반복 | — |
| `OTHER` | 그 외 불만 | — |

### 함께 기록할 항목

| 열 | 값 | 설명 |
|---|---|---|
| `pushback` | Y / N | 사용자 발화별 |
| `pushback_type` | 위 5개 중 | Y인 경우만. 복수면 파이프(\|)로 연결 |
| `abandoned` | Y / N | 참가자 단위. 최소 10턴 조건을 채우지 못하고 대화를 중단했는가 |
| `abandon_reason` | 자유기술 | 첨언에 사유가 있으면 원문 그대로 |

> 프로토콜 미달(10턴 미만)은 데이터에서 제외하지 않는다.
> **이탈 자체가 결과이므로** `abandoned = Y`로 기록하고 논문에 사유와 함께 보고한다.

---

## 13. 제2부 기록 형식

### 에피소드 시트 (`episodes.csv`)

| p_id | rank | topic_self | purpose | purpose_secondary | domain | turn_start | turn_end |
|---|---|---|---|---|---|---|---|
| P06 | 1 | 금요일 강릉 방문 예정 | ES | ADV | FAMILY | 1 | 6 |
| P08 | 1 | 과제 수행에서의 문제 | WORK | | CAREER | 1 | 11 |

`topic_self`는 사후 설문 Q2·Q4에 참가자가 쓴 표현을 요약하지 말고 원문 그대로 옮긴다.

### Pushback 시트 (`pushback.csv`)

| p_id | turn | user_text (일부) | pushback | pushback_type |
|---|---|---|---|---|
| P06 | 5 | 근데 너도 말이 너무 많다… | Y | VERBOSE |
| P06 | 4 | 그래? 근데 야근이 언제… | N | |

---

## 14. 하지 않는 것

- 사용자 감정(VA)을 연구자가 부여하지 않는다. 설문 자기보고를 사용한다.
- 사용자 발화를 문장 단위로 분리하지 않는다.
- 연구자의 인상이나 추론을 pushback 근거로 쓰지 않는다.
- 주제에 대한 분노를 pushback으로 표시하지 않는다.
- pushback 판정과 목적·소재 코드 부여를 LLM에 맡기지 않는다.
- 프로토콜 미달 참가자를 제외하지 않는다.

---

## 15. 신뢰도

- 코더 2인이 독립적으로 코딩한다. 서로의 결과를 보지 않는다.
- Cohen's κ를 계산한다 (목표 0.70 이상).
- `pushback`은 명시적 발화만 대상으로 하므로 κ가 높게 나오는 것이 정상이다.
  낮게 나온다면 "AI를 향한 불만"과 "주제에 대한 분노"를 혼동하고 있을 가능성이 크다.
- 불일치 항목은 조정 회의에서 확정하고, 판정 규칙을 본 문서에 추가한다.
