# ─────────────────────────────────────────────────────────────
# 개인정보 — 절대 커밋하지 않는다
#
# 아래 경로에는 참여자 실명, 나이·성별, 그리고 링크만 있으면 누구나 열람
# 가능한 ChatGPT 공개 share URL이 들어 있다. 원자료는 Git이 아니라 접근
# 통제되는 기관 저장소로만 공유한다.
#
# 공유용 가명 데이터는 `python scripts/deidentify.py` 로 data/deid/ 에 생성한다.
# ─────────────────────────────────────────────────────────────

data/raw/
data/raw_md/
data/survey/
data/private/

# 가명 <-> 실명 연결키가 다른 이름으로 새어 나가는 것을 막는 안전망
*mapping*.csv
*매핑*.csv
*_raw.json
*원본*

# ─────────────────────────────────────────────────────────────
# Python
# ─────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# ─────────────────────────────────────────────────────────────
# 편집기 / OS
# ─────────────────────────────────────────────────────────────
.vscode/
.idea/
Thumbs.db
desktop.ini
.DS_Store
~$*.xlsx
~$*.docx
