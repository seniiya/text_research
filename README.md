# 간접화법 연구 — AI 응답 전략 분석

> **연구 질문** — GPT는 어떤 지원 전략에 분량을 배분하며, 그것이 사용자의 감정 변화와
> 이탈에 어떻게 연결되는가.

참여자 **22명** · 대화 **22건** · 메시지 **598개** (사용자 299 / AI 299) · 에피소드 **28개**

---

## ⚠️ 먼저 읽을 것 — 개인정보

**이 저장소에 실명·연락처·공유 링크를 절대 올리지 않는다.** 참여자는 `P01`~`P22`로만 부른다.

> ⚠️ **`P20`은 2026-07-30 이전 커밋과 다른 사람이다.** 대화 로그를 제출하지 않은
> 참여자 1명을 제외하면서 뒤 번호를 당겼기 때문이다. 그 참여자는 2026-08-04에
> 로그를 제출해 **`P21`로 재합류**했다. 옛 커밋의 `P20`이 지금의 `P21`이다.
> 연결은 `data/private/mapping.csv`로만 확인한다.
>
> **번호 규칙 — 한 번 부여한 `p_id`는 바꾸지 않는다.** 뒤늦게 합류한 응답은 접수
> 순서와 무관하게 다음 빈 번호를 받는다. `deidentify.py`의 `PINNED`에 접수 시각으로
> 고정해 둔다. 접수 순서대로 번호를 다시 매기면 이미 만든 코딩 시트가 전부 다른
> 사람을 가리키게 된다. 위 `P20` 혼란이 그렇게 생겼다.

| 경로 | 상태 | 내용 |
|---|---|---|
| `data/raw/` `data/survey/` `data/private/` | 🔒 **Git 제외** | 구글 스프레드시트 공유 |
| `data/deid/` `data/coding/` | ✅ 공유 대상 | 가명화 완료 |

ChatGPT share URL은 **링크만 알면 누구나 열람**할 수 있다. 채팅에도 붙여넣지 않는다.

`deidentify.py`는 실행할 때마다 산출물 전체를 훑어 실명·URL·이메일·전화번호가
하나라도 남아 있으면 **비정상 종료**한다.

> **공동연구자에게** — 필요한 데이터는 전부 `data/deid/`와 `data/coding/`에 있다.
> 원자료가 필요하면 저장소가 아니라 연구책임자에게 요청한다.

---

## ✅ 지금 할 일

### 1. 제1부 코딩 — AI 응답 전략 `2인 코딩 대기`

`data/coding/<코더>/P##.xlsx`의 **`esconv` 열**을 채운다. → 기준: [코딩북 제1부](codebook_part1.md)

시트는 22명분(`P01`~`P22`) 다 만들어져 있다. 코딩 대상은 총 10,354행.

| 코더 | `P01` | `P06` | 그 외 |
|---|---|---|---|
| coder1 | 183건 완료 | 123건 완료 | 대기 |
| coder2 | 185건 완료 | **다음 차례** | 대기 |

**다음 할 일은 `coder2`의 `P06`이다.** 이게 끝나야 신뢰도를 낼 수 있는 첫 쌍이 나온다.

> ⚠️ **`P01`의 일치도는 보고할 수 없다.** coder1이 `P01` 참여자 본인이다. 본인은
> 자기가 그때 뭘 원했는지 알기 때문에, 텍스트만 보고 판정해야 하는
> `RST`·`RFL`·`QST`가 독립적인 판정이 아니다. 연구자가 2명뿐이라 다른 조합을
> 만들 수도 없다.
>
> 양쪽 라벨 368건(coder1 183 + coder2 185)은 **신뢰도 근거로 쓰지 않는다.**
> 다만 불일치 패턴은 코딩북을 고치는 데 그대로 쓸 수 있다 (아래).

**유효한 파일럿 쌍은 `P06`(124행) + `P03`(142행)이다.** 둘 다 고민 상담 성격이라
정서적 지원 코드가 실제로 나오고, 어느 코더도 본인이 아니다.

#### `P01` 참고 수치와 거기서 드러난 문제

`python scripts/agreement.py P01` → **κ = 0.423** (Po 0.536, 183행 기준).
보고용은 아니지만 코딩북의 구멍은 그대로 보여 준다.

| 문제 | 근거 |
|---|---|
| **`OTH` 판정이 갈린다** | coder1 55건 vs coder2 19건. 코드별 κ 0.297. 전체 불일치 85건 중 34건이 coder1의 `OTH`와 얽혀 있다 |
| **`RST`이 사실상 안 맞는다** | coder1 8 vs coder2 13인데 일치 1건. κ 0.043 |
| **`SLF`은 우연 이하** | κ −0.009. GPT는 자기 경험을 공유할 수 없는데 coder2가 6건 매겼다 |

`OTH`가 핵심이다. 코딩북이 `OTH`에 "목록 제목"을 넣어 둬서, coder1은 짧은 구·
제목을 `OTH`로 보내고 coder2는 내용 코드를 준다 (`당장 추천하는 것` → `OTH`/`SUG`,
`훈수 종료.` → `OTH`/`RST`). **`P06`을 코딩하기 전에 이 경계를 먼저 정해야 한다.**

- 회색 행(`role = Prompt`)은 **사용자 발화**다. 잠겨 있고 코딩 대상이 아니다.
  `RST`·`RFL`·`QST`는 사용자가 뭐라고 했는지 봐야 판정되기 때문에 넣어 둔 것이다.
- 애매하면 **판정 우선순위**(아래 참조)를 위에서부터 적용하고, 이유를 `note`에 적는다.

> ⚠️ **coder2 는 coder1 폴더를 열지 않는다.** 코딩북 제2부 15항이 "서로의 결과를
> 보지 않는다"를 요구한다. 파일 2개와 코딩북만 전달받는 편이 안전하다.
>
> **코더가 참여자 본인인 파일도 열지 않는다.** 위 `P01` 건이 그렇게 생겼다.
> 누가 어느 `P##`인지는 `data/private/mapping.csv`에만 있다.

### 1-2. 제4부 코딩 — 사용자 화행·감정 표면성 `진행 중`

`data/coding/<코더>/P##_speechact.xlsx` → 기준: [코딩북 제4부](codebook_part4_speechact.md)

**`P01`을 뺀 21명 747문장** 전수 시트가 만들어져 있다. 제1부와 단위가 다르다 —
사용자 발화를 **문장 단위로** 나눈 것이며, 코딩 대상은 사용자 쪽이다.

| 코더 | 진행 |
|---|---|
| coder1 | **18명 · 라벨 319문장 (43%) · 제외 50행** |
| coder2 | 대기 |

**현재 분포** (319문장, coder1 1차)

```
S 32% · Q 29% · RESP 17% · EXPR 10% · C 6% · PHATIC 3% · RQ 2% · FRAG 1%
미사용: RC · UNC
축 B   NONE 65% · IMP 21% · EXP 14%
```

**남은 것** — `P21`(84) · `P20`(145) · `P12`(149) = **378문장**. 전체의 51%.
붙여넣기·목록 나열이 많아 규칙 6을 정한 뒤로 미뤘다.

> **참여자별 비율 비교는 `n ≥ 10문장`만** 쓴다 (`P16` 5 · `P17` 1 · `P14` 9 제외).
> 전수 pooled 분석에는 전원 포함하고, 기준선을 논문에 밝힌다.

> `P01`은 모집단에서 제외한다. coder1이 `P01` 참여자 본인이다.
> 다른 참여자도 코더 본인인지 `data/private/mapping.csv`로 먼저 확인한다.

**코딩북이 v2.1이다.** `P04` 파일럿에서 3i4K 6클래스만으로는 `UNC`가 25%까지 올라,
대화행위 표준에서 `PHATIC`·`RESP`·`EXPR` 셋을 보충했다(v2). 이후 코딩하며 반복해서
갈린 경계에 판정 규칙 6개를 붙였다(v2.1). **18명 319문장에서 `UNC` 0% 유지.**
근거는 코딩북 제4부의 「변경 이력」에 있다.

```bash
python scripts/make_speechact_pilot.py --pid=P04 -n=1000   # 전수 시트
python scripts/sync_labels.py --all                        # xlsx -> csv (제1·4부)
python scripts/compare_speechact_versions.py P04           # v1 vs v2 대조표
```

<details>
<summary><b>1차 분석에서 나온 것</b> — 보고서: <code>data/output/report/</code></summary>

**① AI 응답이 길면 사용자가 명시적으로 제지한다.** `P03` 2회 · `P06` 1회, 셋 다 화행은
`C`. 직전 AI 응답이 820 / 404자였다. `C` + 긴 응답을 pushback 자동 탐지 신호로 쓸 수 있다
(제2부 12항 `VERBOSE`).

**② 물음표가 붙었다고 질문이 아니다.** 물음표로 끝나는 78문장 중 **18건(23%)이 `Q`가
아니다.** `~거든?` `~잖아?` 처럼 정보를 제시하며 확인을 구하는 형태가 `P02`·`P07`·`P22`에서
반복된다. 형식으로 자동 분류하면 23%를 놓친다.

**③ 정보 요청형 대화에서는 부정 정서가 발화에 드러나지 않는다.**

| | 대화 정서 | 자기보고 | 주도 화행 |
|---|---|---|---|
| `P06` | 86% | V2 불안함 | `EXPR` 57% |
| `P22` | 50% | V3 분노, 짜증 | `S` 64% |
| **`P02`** | **18%** | **V2 막막함** | **`Q` 59%** |
| **`P19`** | **6%** | **V2 답답하고 막막함** | **`S` 47%** |

자기보고가 부정인 4명이 정반대로 갈린다. `P02`·`P19`는 부정 정서가 없었던 것이 아니라
**정보 요청 형태에서는 표면에 올라오지 않는다.** 로그만 보면 "감정 없는 사용자"가 된다.
연구자가 로그에서 감정을 코딩했다면 이 간극 자체가 사라졌을 것이다 — 제2부 14항이
"사용자 감정(VA)을 연구자가 부여하지 않는다"로 정해 둔 이유가 여기서 확인된다.

**④ 감정은 고민 상담 주제에 몰린다.** 정서 70% 이상은 야근 고민·고민 상담·학생 불만,
20% 미만은 카페 검색·기준금리·과제·스케줄이다.

**두 축이 서로 환원되지 않는다.** `EXPR`이 정서 91%인 것은 당연하지만 `S`(정보 서술)도
31%가 정서를 담고 있고, 그중 28건이 `IMP`(감정어 없이 맥락으로만 읽힘)다. 화행 축만으로는
보이지 않는다.

</details>

<details>
<summary><b>v1 → v2 전이 — 체계가 어디서 무너졌는지</b></summary>

`P04` 28문장을 v1(3i4K 6+UNC)과 v2(9+UNC) 양쪽으로 코딩한 결과다.
**`P04`만 두 벌이 있다.** 나머지는 v2로만 코딩하므로 원 체계 값을 복원할 수 없다.

```
UNC  7  ->  RESP 4 · EXPR 2 · PHATIC 1     무작위가 아니라 셋으로 갈림
S   16  ->  S 12 · RESP 4                   S 에도 응답이 섞여 있었음
Q    4  ->  Q 4        RQ 1 -> RQ 1         원래 맞던 코드는 그대로

UNC 25% -> 0%      코드 변경 11/28 (39%)
```

`Q`·`RQ`가 하나도 안 움직인 것이 핵심이다. 체계 전체가 틀린 게 아니라
**대화행위 층위만 비어 있었다**는 뜻이다.

> 원 체계(3i4K)는 **스마트 스피커 명령 발화**를 대상으로, 억양으로만 갈리는 문장을
> 걸러내려고 만들어졌다. 대화 이력을 의도적으로 배제하므로 "직전 발화에 대한 응답"이
> 구조적으로 없고, 감정 표출은 `S`에 흡수돼 있다. 상담 대화에 그대로 쓸 수 없는
> 이유가 이것이며, **이 불일치 자체가 본 연구의 코딩 기여다.**

</details>

### 2. 제2부 코딩 — 사용자 측 `형식 미정`

→ 기준: [코딩북 제2부](codebook_part2.md)

**아직 아무것도 만들지 않았다.** 제1부(위)를 먼저 끝낸 뒤 형식을 정한다.
코딩북 13항이 `episodes.csv`·`pushback.csv`를 제시하지만, 실제 데이터를 보고
조정할 수 있다. 아래는 검토 중인 사항이다.

<details>
<summary>검토 중 — 무엇이 설문으로 되고 무엇이 안 되는가</summary>

**설문에서 이미 나오는 것** (다시 판정할 필요 없음)

참가자가 사후 설문에 직접 쓴 주제와 에피소드별 감정 점수. 9항 규칙 1도
"참가자 본인의 구분을 우선한다"고 정해 뒀다. 주제2까지 쓴 사람이 6명이라
**에피소드는 총 28개**다 (참여자 22명 × 주제1 + 주제2 6건).

**설문으로 안 되는 것** (판정이 필요하다면 이 셋)

| 항목 | 왜 설문으로 안 되는가 |
|---|---|
| 턴 범위 | 설문은 주제 **이름**만 준다. 몇 번째 턴인지는 없다 |
| `ADV` / `ES` | 설문의 "고민 상담" 하나가 조언 요청과 정서 지지 요청을 안 나눈다 |
| 소재 | 설문에 해당 문항이 없다 |

> 턴 범위가 없으면 **에피소드별 전략 비율을 계산할 수 없다.**
> 설문의 에피소드별 자기보고 감정과 대화 로그를 잇는 유일한 고리다 (9항).

**Pushback은 실제로 드물다.** 1차 11명(사용자 발화 140개)을 훑은 결과 12항 기준을
통과하는 후보는 4건이며, 그중 3건이 응답 길이 문제였다. 전수 판정이 필요한지,
양성 사례만 기록해도 되는지는 제1부를 끝낸 뒤 정한다.

> 표본이 299개로 늘었다(2차 10명 + 3차 1명 추가). 위 4건은 **앞의 140개만 본
> 수치**이므로 나머지 159개를 다시 훑어야 한다.

</details>

### 3. 데이터 정비 `완료`

**대화 22건이 모두 마크다운 기반**이다. 평문으로만 있던 8명분을 `.md`로 다시
내보내 정리를 끝냈다. 이제 같은 GPT 텍스트가 참여자마다 다르게 쪼개지지 않으므로,
**참여자 간 분량 비교가 가능하다.**

`python scripts/deidentify.py`가 실행할 때마다 평문으로 남은 참여자를 경고로 알려준다.
지금은 아무도 걸리지 않는다.

### 4. 확인 필요

- **`후_A`가 `P12`·`P13`·`P14`에서 비어 있다.** 2차 폼 첫날 3명인데, 그 시점에
  폼에 사후 arousal 문항이 없었다. 응답자 탓이 아니라 도구 결측이다.
  **제외하지 않는다** — 결측이 참여자의 상태와 무관하므로, arousal 분석에서만
  `n=19`로 빼고 나머지 분석에는 전원 포함한다.
- **`OTH` 경계를 정해야 한다.** `P01`에서 coder1 30%(55/183) vs coder2 10%(19/183).
  7항 진단선이 30%인데, 이게 데이터 문제인지 판정 기준 문제인지 갈린다.
- `SLF`는 GPT가 자기 경험을 공유할 수 없어 구조적으로 관찰되기 어렵다. ESConv
  원 데이터에서는 약 9.7%를 차지하는 전략이다. `P01`에서 coder2가 6건 매긴 것을
  조정 회의에서 확인해야 한다.

---

## 📋 코드 빠른 참조

코딩북을 열지 않고 확인하는 용도다. **판정 규칙과 경계 사례는 코딩북이 기준이다.**

### 제1부 — ESConv 8전략 → `esconv` 열

<table>
<tr><th>축</th><th>코드</th><th>전략</th><th>정의</th></tr>
<tr><td rowspan="3"><b>정서적<br>지원</b></td>
    <td><code>RFL</code></td><td>감정 반영</td><td>사용자의 감정을 짚어 말해 줌</td></tr>
<tr><td><code>AFF</code></td><td>인정·안심</td><td>사용자를 긍정하거나 걱정을 덜어 줌</td></tr>
<tr><td><code>SLF</code></td><td>자기개방</td><td>화자 자신의 경험·감정을 공유</td></tr>
<tr><td rowspan="2"><b>도구적<br>지원</b></td>
    <td><code>SUG</code></td><td>제안 제시</td><td>구체적 행동·해결책 제안</td></tr>
<tr><td><code>INF</code></td><td>정보 제공</td><td>사실·원인·수치 등 정보 전달</td></tr>
<tr><td rowspan="2"><b>탐색</b></td>
    <td><code>QST</code></td><td>질문</td><td>사용자에게 정보나 감정을 되묻는 발화</td></tr>
<tr><td><code>RST</code></td><td>재진술</td><td>사용자가 한 말을 다시 정리해 되돌려 줌</td></tr>
<tr><td><b>기타</b></td><td><code>OTH</code></td><td>기타</td><td>인사, 농담, 이모지, 목록 제목 등</td></tr>
</table>

**판정 우선순위** — 위에서부터 먼저 걸리는 규칙을 따른다 (6항)

```
1. 물음표로 끝난다                        → QST
2. 사용자 말을 되돌려 정리한다              → RST
3. 감정어(힘들겠다·이해돼·답답하지)가 중심   → RFL
4. 걱정을 덜어 주거나 긍정한다              → AFF
5. "~해봐", "~하는 게 좋아"                → SUG
6. 사실·원인·수치를 설명한다                → INF
7. 위에 해당 없음                          → OTH
```

> ⚠️ **`OTH`가 30%를 넘으면** 분류 체계가 데이터에 맞지 않는다는 뜻이다 (7항).
> `OTH` 내용을 재검토하고 논문에 한계로 보고한다.

### 제4부 — 사용자 화행·감정 표면성 → `P##_speechact.xlsx`

단위가 **사용자 발화를 문장으로 나눈 것**이다. 출처: [코딩북 제4부](codebook_part4_speechact.md)

| 코드 | 기준 | 코드 | 기준 |
|---|---|---|---|
| `FRAG` | 절이 완결되지 않음 (길이 무관) | `Q` | 실제로 답을 요구 |
| `RESP` | 직전 AI 발화에 대한 대답·수락·거절 | `C` | 행동·수행을 요구 |
| `PHATIC` | 인사·작별·감사·사과 | `RQ` | 의문문 형식, 답 요구 없음 |
| `EXPR` | 감정·평가의 분출이 목적 | `RC` | 명령문 형식, 지시 아님 |
| `S` | 정보·상태 서술 | `UNC` | 어디에도 안 맞음 (이유를 note에) |

**판정 우선순위** — 위에서부터 먼저 걸리는 규칙을 따른다

```
FRAG → RESP → PHATIC → EXPR → Q → C → RQ → RC → S → UNC
```

> **`RESP`가 `EXPR`보다 앞선다.** 정서는 축 B가 이미 잡으므로, 축 A까지 정서를
> 기준으로 두면 두 축이 같은 것을 재게 된다.

**축 B 감정 표면성** — `EXP` 감정어가 표면에 명시 · `IMP` 맥락상 읽힘 · `NONE` 정서 무관
(`IMP`와 `UNC`는 `note`에 근거를 적는다)

<details>
<summary><b>제2부 — 사용자 측 코드</b> (대화 목적 · 소재 · pushback)</summary>

제1부와 코드 체계가 다르다. 출처: [코딩북 제2부](codebook_part2.md)

**대화 목적** — 에피소드 단위 (10항). 사전 설문 Q4 선택지를 그대로 사용

| 코드 | 목적 | 코드 | 목적 |
|---|---|---|---|
| `INFO` | 정보 검색·질문 | `ADV` | 고민 상담 → **조언** 요청 |
| `WORK` | 학업·업무 도움 | `ES` | 고민 상담 → **정서적 지지** 요청 |
| `WRITE` | 글쓰기 도움 | `PLAY` | 창작·놀이 |
| `CODE` | 코딩·기술 | `ETC` | 기타 |
| `CHAT` | 일상 잡담 | | |

> **`ADV` vs `ES` 구분이 이 연구의 코딩 기여다.** 설문의 "고민 상담" 하나를 둘로 쪼갠 것.
> 의문형·방법 요청이면 `ADV`, 서술형·감정 진술 중심이면 `ES`.

**소재** — 에피소드 단위 (11항). 목적은 "무엇을 원했나", 소재는 "무엇에 관한 이야기인가"

`HEALTH` 건강·신체 · `CAREER` 진로·학업 · `SOCIAL` 대인관계 · `FAMILY` 가족
`JOB` 직무·감정노동 · `DAILY` 일상·취미 · `MONEY` 금전 · `ETC` 기타

**Pushback** — 사용자 발화 단위 (12항). AI **응답 방식**에 대한 명시적 불만만 `Y`

| 코드 | 유형 |
|---|---|
| `VERBOSE` | 응답이 너무 길다 |
| `UNSOL_ADV` | 요청하지 않은 조언·해결책 |
| `CONTRA` | 사용자 진술을 반박·정정함 |
| `REPEAT` | 같은 말 반복 |
| `OTHER` | 그 외 불만 |

> ⚠️ 대화 **주제**에 대한 분노(학생·가족에 대한 짜증)는 pushback이 **아니다**.
> 연구자가 "답답해 보인다"고 추론한 것도 `Y`로 하지 않는다. **명시적으로 언어화된 것만**이다.

</details>

---

## 📁 디렉터리

```
data/
  deid/                  ✅ 가명화 완료
    chats/P01~P22.json         대화 로그 22건
    survey_analysis.csv        설문 분석표 22행
    index.csv                  P## → source, 제목, 메시지 수
  coding/                ✅ 코딩 시트 (CSV + XLSX)
    coder1/  P##.xlsx          제1부 — P01 183 · P06 123 완료
             P##_speechact.xlsx  제4부 — 18명 319문장 완료 (P01 제외 21명분)
             P04_speechact_v1.xlsx  제4부 v1 보존본. 입력하지 않는다
    coder2/                    제1부 P01 185 완료 · 제4부 전부 대기
  output/                   분석 산출물 (스크립트가 만든다. 직접 고치지 않는다)
    pilot_speechact/
      P04_v1_v2_compare.csv    제4부 v1 vs v2 대조표
    report/                    교수님 보고용 (손으로 만든다. 재생성 안 됨)
      화행코딩_1차보고_*.docx     발견 4개 + 차트 4장
      c1~c4.png                차트 원본

  raw/                   🔒 원본 대화 (.md / .json, 파일명에 실명)
  survey/                🔒 설문 원본
  private/               🔒 가명–실명 연결키

scripts/                  ← 아래 「스크립트가 뭘 하는가」 참조
codebook_part1~4.md       ← 코딩 기준 문서 4개

notes/
  observations.md           여러 턴에 걸친 질적 관찰 (등급·상태 표기 필수)
```

### 스크립트가 뭘 하는가

**언제 쓰는지**를 기준으로 정리했다. 순서대로 읽으면 데이터 흐름이 그대로 나온다.

| 언제 | 스크립트 | 하는 일 |
|---|---|---|
| ① 참여자가 새로 들어왔을 때 | `deidentify.py` | 원자료 → `data/deid/`. 실명·URL 치환하고 남아 있으면 **비정상 종료**. `p_id`도 여기서 배정 |
| ② 시트를 만들 때 | `make_coding_sheet.py` | 제1부(AI 응답 전략) 시트 |
| | `make_speechact_pilot.py` | 제4부(사용자 화행) 시트 |
| ③ 코딩한 뒤 | `sync_labels.py` | XLSX 에 입력한 라벨 → 같은 이름 CSV. **정본은 XLSX**라 이걸 돌려야 Git 에 보인다 |
| ④ 분석할 때 | `agreement.py` | 코더 간 일치도 (Cohen's κ) |
| | `compare_speechact_versions.py` | 제4부 v1 vs v2 대조표 |
| | `va_structure.py` | 설문 자기보고 VA 세 값(전/주제1/후)의 구조 확인 |

**직접 실행하지 않는 것 2개** — 위 스크립트들이 안에서 불러 쓴다.

| | |
|---|---|
| `markdown_chat.py` | 마크다운 구조를 살려 코딩 단위를 만든다. GPT 가 한 문장을 여러 문단에 걸쳐 쓰는 걸 다시 이어 붙이는 부분 |
| `split_sentences.py` | 문장 분리 규칙(제1부 3항). 단독 실행하면 분리 결과 **검증 리포트**만 찍는다 (파일 안 만듦) |

### 코딩 시트 파일 이름 읽는 법

`data/coding/<코더>/` 안에 참여자마다 여러 파일이 있다. **이름이 축을 가른다.**

| 파일 | 무엇 | 코딩 대상 |
|---|---|---|
| `P04.xlsx` | 제1부 — AI 응답 전략 | `role=Response` 행 |
| `P04_speechact.xlsx` | 제4부 — 사용자 화행 | 사용자 발화를 문장으로 나눈 것 |
| `P04_speechact_v1.xlsx` | 제4부 **v1 보존본** | 입력하지 않는다. 대조표 근거 |
| `*.csv` | 같은 내용의 Git 확인용 사본 | 입력하지 않는다 |

> **입력은 항상 `.xlsx`에 한다.** CSV 는 `sync_labels.py` 를 돌려야 따라온다.

---

<details>
<summary><b>📊 데이터 형식</b></summary>

### `data/coding/P##.csv` / `.xlsx`

| 열 | 값 |
|---|---|
| `p_id` | `P01` ~ `P20` |
| `turn` | 몇 번째 주고받기인지 (1부터) |
| `sent_no` | 그 turn 안에서 몇 번째 문장인지. 문맥 행은 `-` |
| `role` | `Prompt` 사용자(문맥) / `Response` AI(코딩 대상) |
| `text` | 문장 |
| `chars` | `text`의 글자 수 (공백 포함) |
| `esconv` | **입력란.** 8코드 중 하나. 문맥 행은 `-` |
| `note` | **입력란.** 판정이 애매했던 이유. 자유기술 |

XLSX에는 드롭다운·문맥 행 잠금·시트 보호가 걸려 있어 코딩 대상 셀에만 입력된다.
규칙을 바꾸려면 `[검토] → [시트 보호 해제]`.

### `data/deid/chats/P##.json`

```json
{
  "metadata": {
    "title": "대화 제목",
    "user": { "name": "P04" },
    "dates": { "created": "…", "updated": "…", "exported": "…" },
    "format": "markdown"
  },
  "messages": [
    { "role": "Prompt",   "say": "사용자 발화", "time": "…" },
    { "role": "Response", "say": "AI 응답",    "time": "…" }
  ]
}
```

`format`이 `markdown`이면 `say`에 구조 표시가 살아 있고, `plain`이면 문단만 남아 있다.
**현재 22건 모두 `markdown`이다.** 코딩 대상은 `Response`뿐이다 (제1부 2항).

대화 본문에 참여자 실명이 남아 있으면 `deidentify.py`가 `[이름]`으로 바꾼다.
GPT가 성을 뗀 이름으로 부르는 일이 잦아서(`"○○이는 22살이지만"`) 풀네임만으로는
걸러지지 않는다.

</details>

<details>
<summary><b>🔧 데이터 재생성</b> (원자료를 가진 사람만)</summary>

```bash
python scripts/deidentify.py                 # 원자료 → data/deid/
python scripts/make_coding_sheet.py P01                 # → data/coding/coder1/
python scripts/make_coding_sheet.py P01 --coder=coder2  # → data/coding/coder2/
python scripts/split_sentences.py P01        # 분리 검증 리포트 (파일 안 만듦)

python scripts/sync_labels.py --all          # 엑셀에 입력한 라벨 → CSV 반영
python scripts/agreement.py P06              # coder1 vs coder2 일치도
```

> **라벨은 XLSX 가 정본이다.** 코더가 엑셀에서 작업하므로, CSV 는 시트를 만든
> 시점에 멈춰 있다. Git 으로 내용을 보려면 `sync_labels.py` 를 먼저 돌린다.

> `make_coding_sheet.py`는 XLSX 생성에 **openpyxl**이 필요하다.
> 이 프로젝트에는 자체 venv가 없으므로 openpyxl이 설치된 인터프리터로 실행한다.

### 마크다운을 쓰는 이유

GPT는 **한 문장을 여러 문단에 걸쳐 쓰는 일이 잦다.** JSON 내보내기는 마크다운 구조를
버리기 때문에 문단마다 끊으면 문장이 조각난다.

```
'여기서는' / 'LLM을' / '"잘 만드는 것"' / '보다' / '"잘 사용하는 것"' / '이 중요하다.'
```

마크다운에는 리스트(`- `), 제목(`## `), 인용(`> `), 표(`| `) 표시가 남아 있어
**진짜 목록 항목과 끊긴 문단을 구분**할 수 있다. 끊긴 문단만 다시 이어 붙인다.

```
'여기서는 LLM을 "잘 만드는 것"보다 "잘 사용하는 것"이 중요하다.'
```

같은 사람의 `.md`와 `.json`이 둘 다 있으면 `.md`를 쓴다. 어느 쪽을 썼는지는
`data/deid/index.csv`의 `source` 열에 기록된다.

### 설문 CSV 구조 주의

설문 원본이 **두 벌**이다. 1차 폼은 한 파일에 표가 두 개 세로로 쌓여 있고,
2차 모집은 폼을 새로 만들어 열 구성이 다르다. 스크립트가 둘을 합쳐 하나의
분석표로 만든다.

| 출처 | 위치 | 내용 |
|---|---|---|
| 1차 표 A | 0행 헤더, 1~11행 | 접수 응답. 실명·share URL 포함 → **연결키 생성에만** 사용 |
| 1차 표 B | `p_id` 헤더 행 이후 | 분석표. 이미 `P01`~`P11` 기준 → 그대로 공유 |
| 2차 폼 | 별도 CSV, 헤더 1줄 | 접수 순서대로 `P12`부터 부여. 열 이름을 표 B에 맞춰 변환 |

설문 응답을 연구자가 바로잡은 내역은 **`deidentify.py`의 `CORRECTIONS`에만** 적는다.
`survey_analysis.csv`를 직접 고치면 다음 실행에서 덮어써진다 — 스크립트가 이 파일을
매번 통째로 다시 만들기 때문이다. 손으로 고친 값이 발견되면 실행 중 경고가 뜬다.

</details>

---

**전략 분류 출처** — Liu et al. (2021), *Towards Emotional Support Dialog Systems*, ACL 2021
