# 코딩북 제1부 — AI 응답 전략 코딩

> 이 문서는 참조용 기준 문서다. 지시는 별도 프롬프트로 전달한다.
> 이 문서를 읽었다는 이유만으로 라벨링을 수행하지 말 것.

---

## 1. 목적

수집된 ChatGPT 응답이 8가지 지원 전략 중 어디에 분량을 배분했는지 측정한다.
각 전략의 질적 깊이나 적절성은 판정하지 않는다. 분량 배분만 측정하며, 지원의 효과는
참가자 자기보고(사후 설문·첨언)로 판단한다.

## 2. 코딩 단위

- 문장 1개 = 코드 1개. 한 문장 안을 쪼개지 않는다.
- AI 응답만 코딩한다. 사용자 발화는 제1부의 대상이 아니다.
- 한 문장에 두 기능이 섞이면 주된 기능 하나만 부여한다.

---

## 3. 문장 분리 규칙

GPT는 마침표 없이 줄바꿈과 불릿으로 쓰는 경우가 많다. 따라서 마침표가 아니라
줄바꿈을 1차 기준으로 삼는다. 아래 순서대로 적용한다.

1. 먼저 줄바꿈(`\n`)으로 나눈다.
2. 나뉜 각 줄 안에 마침표·물음표·느낌표가 2개 이상 있으면 추가로 나눈다.
3. 구두점이 없는 줄(불릿 항목, 목록 제목 등)도 1개 문장으로 센다.
4. 빈 줄과 공백만 있는 줄은 제외한다.
5. 이모지만 있는 줄은 1개 문장으로 유지한다.

### 분리 예시

| 원문 | 분리 결과 | 참고 코드 |
|---|---|---|
| `그렇게 느껴질 만해. 특히 요즘 활동량이 줄었다면 흔해.` | 2개 (마침표로 추가 분리) | RFL / INF |
| `활동량 감소 → 하루 소비 칼로리가 줄어 체지방이 증가.` | 1개 | INF |
| `매일 30분 정도 걷기` | 1개 (구두점 없어도 1개) | SUG |
| `당장 추천하는 것` | 1개 (목록 제목) | OTH |
| `🫡` | 1개 | OTH |

### 분리 시 주의할 문자

아래는 문장 경계로 취급하지 않는다. 잘못 나뉘기 쉬운 지점이다.

- 화살표 `→`, `~`, `-` : 한 문장 안의 연결 기호
- 소수점, 시간 표기의 마침표·콜론 (`3.5시간`, `1:30`)
- 줄임표 `...`, `…`
- 괄호 안의 문장부호
- 불릿 기호 `•`, `-`, `*`, 번호 `1.`, `①` : 제거하고 본문만 남긴다

### 분리하지 않는 것

- 사용자 발화(`role: Prompt`)는 분리하지 않는다. 구두점을 일관되게 쓰지 않아
  자동 분리가 부정확하다.
- 분리 작업은 스크립트로 처리하며 코더가 손으로 나누지 않는다.

---

## 4. 코드 정의 — ESConv 8 전략

| 코드 | 전략 (원문) | 정의 | 예시 |
|---|---|---|---|
| `QST` | Question / 질문 | 사용자에게 정보나 감정을 되묻는 발화 | "오늘 빡센 게 수업 개수가 많아서야?" |
| `RST` | Restatement or Paraphrasing / 재진술 | 사용자가 한 말을 다시 정리해 되돌려 줌 | "네 말의 진짜 의미는 알 것 같아. 설명을 한 번 해도 잘 안 되고 계속 붙잡고 있어야 하는 애들이 많다는 거잖아." |
| `RFL` | Reflection of Feelings / 감정 반영 | 사용자의 감정을 짚어 말해 줌 | "그렇게 느껴질 만해." / "읽는데 진짜 스트레스가 여기까지 느껴진다." |
| `SLF` | Self-disclosure / 자기개방 | 화자 자신의 경험·감정을 공유 | (수집 데이터에서 거의 관찰되지 않음) |
| `AFF` | Affirmation and Reassurance / 인정·안심 | 사용자를 긍정하거나 걱정을 덜어 줌 | "너무 빨리 살이 많이 쪘다고 단정할 필요는 없어." |
| `SUG` | Providing Suggestions / 제안 제시 | 구체적 행동·해결책 제안 | "매일 30분 정도 걷기" / "목베개 챙기고 이어폰 끼고 눈이라도 붙여." |
| `INF` | Information / 정보 제공 | 사실·원인·수치 등 정보 전달 | "활동량 감소로 하루 소비 칼로리가 줄어 체지방이 증가." |
| `OTH` | Others / 기타 | 인사, 농담, 이모지, 목록 제목 등 | "ㅋㅋㅋㅋ 인정." / "🫡" |

출처: Liu et al. (2021), Towards Emotional Support Dialog Systems, ACL 2021.

---

## 5. 분석용 상위 묶음

8개 코드를 그대로 기록하되, 분석에서는 아래 3개 축으로 묶는다.

| 상위 축 | 포함 코드 | 성격 |
|---|---|---|
| 정서적 지원 (affective) | RFL, AFF, SLF | 감정을 다룸 |
| 도구적 지원 (instrumental) | SUG, INF | 문제를 다룸 |
| 탐색 (exploration) | QST, RST | 정보를 끌어냄 |
| 기타 | OTH | — |

---

## 6. 판정 우선순위 (애매할 때)

위에서부터 순서대로 적용한다. 먼저 걸리는 규칙을 따른다.

1. 물음표로 끝나면 → `QST`
2. 사용자 말을 되돌려 정리하면 → `RST`
3. 감정어(힘들겠다, 이해돼, 답답하지)가 중심이면 → `RFL`
4. 걱정을 덜어 주거나 긍정하면 → `AFF`
5. "~해봐", "~하는 게 좋아" → `SUG`
6. 사실·원인·수치 설명 → `INF`
7. 위에 해당 없음 → `OTH`

### 경계 사례

| 상황 | 처리 |
|---|---|
| "걱정할 필요 없어" | `AFF` (안심시키기) |
| "너무 걱정 말고 2주간 기록해봐" | `SUG` (행동 지시가 주된 기능) |
| 조언 내용인데 질문형 | `QST` 우선 |
| 목록 제목 ("당장 추천하는 것") | `OTH` |
| 이모지만 있는 줄 | `OTH` |
| 판단 불가 | `OTH`로 두고 confidence를 low로 표시 |

---

## 7. 분류 체계 적합도 진단

ESConv 전략 체계는 정서지원 대화를 위해 개발되었다. 본 데이터에는 정보탐색·과업
목적 대화도 포함되므로, 체계가 데이터에 맞는지 자체 점검이 필요하다.

| OTH 비율 | 해석 | 조치 |
|---|---|---|
| 20% 미만 | 체계가 잘 맞음 | 그대로 진행 |
| 20~30% | 허용 범위 | 논문에 비율 명시 |
| 30% 초과 | 체계와 데이터가 불일치 | OTH 내용을 재검토하고 한계로 보고 |

---

## 8. 기록 형식

| p_id | turn | sent_no | text | code | chars |
|---|---|---|---|---|---|
| P06 | 1 | 1 | 그렇게 느껴질 만해. | RFL | 11 |
| P06 | 1 | 2 | 활동량 감소로 체지방이 증가. | INF | 17 |
| P06 | 1 | 3 | 매일 30분 정도 걷기 | SUG | 12 |

- `chars`는 스크립트로 자동 계산한다. 손으로 세지 않는다.
- `turn`은 몇 번째 주고받기인지 (1부터).
- `sent_no`는 해당 turn 안에서 몇 번째 문장인지 (1부터).

---

## 9. 하지 않는 것

- 한 문장 안에서 비율을 나누지 않는다.
- 각 전략의 질적 깊이나 적절성을 판정하지 않는다.
- 사용자 발화를 문장 단위로 분리하지 않는다.
- 사용자 감정(VA)이나 불만(pushback) 여부를 판정하지 않는다. 제1부의 대상이 아니다.
- 코딩북에 없는 새 코드를 만들지 않는다.
